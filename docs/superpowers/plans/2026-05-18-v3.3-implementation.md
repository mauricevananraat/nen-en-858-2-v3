# v3.3 — UX-finish + CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vijf kleine verbeteringen op v3 (live) plus 1 cleanup-item: voorpagina geen footer, §6 thumbnails kleiner via size-parameter, spinner safety-net 20s, GitHub Actions CI workflow, skipped sync-ui-test verwijderen.

**Architecture:** Bestaande pdf-builder + main.js krijgen kleine wijzigingen. Eén nieuw bestand: `.github/workflows/test.yml`. Eén test-blok verwijderd. Geen architectuur-veranderingen, geen breaking changes, geen data-migratie.

**Tech Stack:** Vanilla JS ES modules, vitest, GitHub Actions, pdfMake, Playwright voor visuele PDF-verificatie.

**Spec:** `docs/superpowers/specs/2026-05-18-v3.3-ux-finish-ci-design.md` (commit `40d3eca`)
**Baseline:** 277 tests groen + 1 skipped, commit `40d3eca` op `main`
**Eindstand:** 277 tests groen + 0 skipped

---

## File Structure

### Nieuw
| Pad | Verantwoordelijkheid |
|-----|----------------------|
| `.github/workflows/test.yml` | CI: draait `npm test` bij push naar main + alle PRs |

### Gewijzigd
| Pad | Wijziging |
|-----|-----------|
| `js/pdf-builder.js` | Footer-callback skip op pagina 1 + paginanummer N-1; `photoBlock` krijgt `size` param; §6 photoBlock-calls met `size: 110` |
| `js/main.js` | `PDF_SPINNER_TIMEOUT_MS` constante = 20000 bovenaan; safety-net setTimeout gebruikt constante |
| `tests/sync-ui.test.js` | `describe('bindSyncButtons — import-mode dialog')` blok cleanup: skipped test verwijderd, idempotent-test behouden in eigen describe |
| `STATUS.md` | v3.3-sectie toevoegen onderaan |

---

## Task 1: Skipped sync-ui-test verwijderen (cleanup)

**Files:**
- Modify: `tests/sync-ui.test.js`

Volgorde-keuze: eerst de cleanup om met 0 skipped tests verder te werken.

- [ ] **Step 1.1: Lees de huidige test-structuur**

Open `tests/sync-ui.test.js` en zoek het blok rond regel 161:

```js
describe('bindSyncButtons — import-mode dialog', () => {
  beforeEach(() => localStorage.clear());

  it.skip('vraagt mode via confirm bij bestand-selectie', async () => {
    saveDb({ versie: 1, klanten: [], voorzieningen: [] });
    makeContainer();
    bindSyncButtons();

    let confirmCalled = false;
    const origConfirm = window.confirm;
    window.confirm = () => { confirmCalled = true; return false; };
    try {
      expect(typeof document.getElementById('btn-import-db').onclick === 'function'
        || document.getElementById('btn-import-db').addEventListener).toBeTruthy();
    } finally {
      window.confirm = origConfirm;
    }
  });

  it('is idempotent — 2x bindSyncButtons aanroepen voegt niet dubbele listeners toe', () => {
    makeContainer();
    bindSyncButtons();
    bindSyncButtons();
    expect(document.body.dataset.syncButtonsBound).toBe('1');
  });
});
```

- [ ] **Step 1.2: Verwijder het hele `it.skip(...)` blok**

Het beoogde resultaat is:

```js
describe('bindSyncButtons — idempotency', () => {
  beforeEach(() => localStorage.clear());

  it('is idempotent — 2x bindSyncButtons aanroepen voegt niet dubbele listeners toe', () => {
    makeContainer();
    bindSyncButtons();
    bindSyncButtons();
    expect(document.body.dataset.syncButtonsBound).toBe('1');
  });
});
```

Wijzigingen:
- describe-naam: `'bindSyncButtons — import-mode dialog'` → `'bindSyncButtons — idempotency'` (semantischer nu)
- `it.skip(...)`-blok volledig weg (regels 164-182 in originele)
- `it('is idempotent...')` blijft staan

- [ ] **Step 1.3: Run tests**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm test 2>&1 | tail -5
```

Expected: `277 passed | 0 skipped (277)`. Was 277+1=278, nu 277 zonder skip.

- [ ] **Step 1.4: Commit**

```bash
git add tests/sync-ui.test.js
git commit -m "$(cat <<'EOF'
test(cleanup): verwijder obsolete it.skip — import-mode dialog

Test was uit v2 fase 5 toen mode-keuze via confirm() ging. In v3.1
is dat vervangen door openImportModeModal (custom modal) met eigen
tests in tests/import-mode-modal.test.js. De skipped test bevatte
alleen een tautologie-assertie en gaf geen coverage.

Resultaat: 277 + 0 skipped (was 277 + 1 skipped).
Describe hernoemd naar 'bindSyncButtons — idempotency' (focus).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: GitHub Actions CI workflow

**Files:**
- Create: `.github/workflows/test.yml`

- [ ] **Step 2.1: Maak de workflow-file**

```bash
mkdir -p "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3/.github/workflows"
```

Maak `.github/workflows/test.yml` met inhoud:

```yaml
name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test
```

NOTE: gebruik `npm ci` (deterministisch via package-lock) i.p.v. `npm install`. `cache: 'npm'` versnelt subsequent runs door npm-cache te bewaren tussen workflow-runs.

- [ ] **Step 2.2: Verifieer dat lokaal `npm ci` werkt**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm ci 2>&1 | tail -5
```

Expected: geen errors. Als `npm ci` hier faalt, faalt de CI ook. Bij faal: rapporteer aan controller — package-lock kan corrupt zijn.

- [ ] **Step 2.3: Run tests om baseline te bevestigen**

```bash
npm test 2>&1 | tail -5
```

Expected: 277 passed | 0 skipped.

- [ ] **Step 2.4: Commit + push**

```bash
git add .github/workflows/test.yml
git commit -m "$(cat <<'EOF'
ci(github): voeg test.yml workflow toe (push + PR)

GitHub Actions draait npm ci + npm test bij elke push naar main
en elke pull request. Voorkomt regressie-merges. Node 20 met
npm-cache voor snellere subsequent runs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

- [ ] **Step 2.5: Verifieer dat Actions-tab op GitHub groen wordt**

Open `https://github.com/mauricevananraat/nen-en-858-2-v3/actions` na ~30 sec. Verwacht: nieuwe run met titel "ci(github): voeg test.yml workflow toe" met groene checkmark binnen ~1 min.

Bij rode X: open de run, lees logs, fix issue lokaal en commit fix.

---

## Task 3: Spinner safety-net 20s + constante

**Files:**
- Modify: `js/main.js`

- [ ] **Step 3.1: Lees huidige main.js btn-pdf handler**

Vind de regels rond `setTimeout(hideSpinner, 10000)`. Verwacht patroon:

```js
document.getElementById('btn-pdf').addEventListener('click', () => {
  // ... foto-confirm logic ...
  showSpinner('Bezig met genereren van PDF...');
  try {
    const dd = buildDocDefinition(state);
    const naam = `inspectie-${state.meta.projectnummer || 'rapport'}-${state.meta.rapportagedatum}.pdf`;
    pdfMake.createPdf(dd).download(naam, () => hideSpinner());
    setTimeout(hideSpinner, 10000);
  } catch (e) {
    hideSpinner();
    showToast('PDF-generatie mislukt: ' + e.message, 'error');
  }
});
```

- [ ] **Step 3.2: Voeg constante toe bovenaan main.js**

Direct na de bestaande imports (vóór `const state = ...` of vóór andere const-declaraties op top-level), voeg toe:

```js
const PDF_SPINNER_TIMEOUT_MS = 20000;
```

NOTE: plaats logisch tussen imports en eerste runtime-statement. Als er al een constants-blok is, voeg daar toe.

- [ ] **Step 3.3: Vervang magic number door constante**

Vind:
```js
setTimeout(hideSpinner, 10000);
```

Vervang door:
```js
setTimeout(hideSpinner, PDF_SPINNER_TIMEOUT_MS);
```

- [ ] **Step 3.4: Run tests**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm test 2>&1 | tail -5
```

Expected: 277 passed | 0 skipped.

- [ ] **Step 3.5: Commit + push**

```bash
git add js/main.js
git commit -m "$(cat <<'EOF'
feat(spinner): safety-net 10s → 20s via constante

PDF-spinner verdwijnt nu auto na 20 sec (was 10). pdfMake.download
callback firet bij grote rapporten op trage devices niet altijd
binnen 10 sec — 20s geeft ruimere marge zonder eindeloos te hangen.
Magic number 10000 vervangen door PDF_SPINNER_TIMEOUT_MS constante
bovenaan main.js voor 1 plek om te tunen.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 4: photoBlock size-parameter + §6 thumbnails 110px

**Files:**
- Modify: `js/pdf-builder.js`

- [ ] **Step 4.1: Voeg size-parameter toe aan photoBlock**

Vind in `js/pdf-builder.js` rond regel 40:

```js
function photoBlock(photos, title) {
  if (!photos?.length) return null;
  return {
    stack: [
      { text: title, fontSize: 9, italics: true, color: '#555', margin: [0, 6, 0, 4] },
      {
        columns: photos.map(p => ({
          stack: [
            // fit: [140, 140] dwingt 1:1 vakje af, ook bij oudere foto's met andere aspect ratio
            { image: p.dataurl, fit: [140, 140] },
            ...(p.bijschrift ? [{ text: p.bijschrift, fontSize: 8, italics: true, alignment: 'center', margin: [0, 2] }] : [])
          ],
          width: 150,
          margin: [0, 0, 6, 0]
        }))
      }
    ],
    unbreakable: true
  };
}
```

Vervang door:

```js
function photoBlock(photos, title, size = 140) {
  if (!photos?.length) return null;
  const colWidth = size + 10; // kolom iets ruimer dan thumbnail
  return {
    stack: [
      { text: title, fontSize: 9, italics: true, color: '#555', margin: [0, 6, 0, 4] },
      {
        columns: photos.map(p => ({
          stack: [
            // fit: [size, size] dwingt 1:1 vakje af, ook bij oudere foto's met andere aspect ratio
            { image: p.dataurl, fit: [size, size] },
            ...(p.bijschrift ? [{ text: p.bijschrift, fontSize: 8, italics: true, alignment: 'center', margin: [0, 2] }] : [])
          ],
          width: colWidth,
          margin: [0, 0, 6, 0]
        }))
      }
    ],
    unbreakable: true
  };
}
```

- [ ] **Step 4.2: §6 photoBlock-calls krijgen size: 110**

Vind in `js/pdf-builder.js` de §6-functie (rond regel 575-586):

```js
return sectionCard('6. INWENDIGE CONTROLE (na lediging)', [
    ...(wandenRows.length      ? [subBlock('WANDEN + BODEM',        wandenRows)]     : []),
    ...(schottenRows.length    ? [subBlock('SCHOTTEN / VLOTTERKOKER', schottenRows)] : []),
    ...(coalescentieRows.length? [subBlock('COALESCENTIEFILTER',     coalescentieRows)] : []),
    ...(afsluiterRows.length   ? [subBlock('AFSLUITER MECHANISCH',   afsluiterRows)] : []),
    ...(nadenRows.length       ? [subBlock('NADEN / AANSLUITINGEN', nadenRows)]     : []),
    photoBlock(state.fotos.inwendig_wanden,       "Foto's wanden + bodem:"),
    photoBlock(state.fotos.inwendig_schotten,     "Foto's schotten / vlotterkoker:"),
    photoBlock(state.fotos.inwendig_coalescentie, "Foto's coalescentiefilter:"),
    photoBlock(state.fotos.inwendig_afsluiter,    "Foto's afsluiter mechanisch:"),
    photoBlock(state.fotos.inwendig_naden,        "Foto's naden / aansluitingen:")
  ]);
```

Vervang elke `photoBlock(...)`-call met de 3e parameter `110`:

```js
return sectionCard('6. INWENDIGE CONTROLE (na lediging)', [
    ...(wandenRows.length      ? [subBlock('WANDEN + BODEM',        wandenRows)]     : []),
    ...(schottenRows.length    ? [subBlock('SCHOTTEN / VLOTTERKOKER', schottenRows)] : []),
    ...(coalescentieRows.length? [subBlock('COALESCENTIEFILTER',     coalescentieRows)] : []),
    ...(afsluiterRows.length   ? [subBlock('AFSLUITER MECHANISCH',   afsluiterRows)] : []),
    ...(nadenRows.length       ? [subBlock('NADEN / AANSLUITINGEN', nadenRows)]     : []),
    photoBlock(state.fotos.inwendig_wanden,       "Foto's wanden + bodem:",       110),
    photoBlock(state.fotos.inwendig_schotten,     "Foto's schotten / vlotterkoker:", 110),
    photoBlock(state.fotos.inwendig_coalescentie, "Foto's coalescentiefilter:",      110),
    photoBlock(state.fotos.inwendig_afsluiter,    "Foto's afsluiter mechanisch:",    110),
    photoBlock(state.fotos.inwendig_naden,        "Foto's naden / aansluitingen:",   110)
  ]);
```

Behoud de bestaande indentatie/uitlijning waar mogelijk.

- [ ] **Step 4.3: Run tests**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm test 2>&1 | tail -5
```

Expected: 277 passed | 0 skipped (geen test-impact — alleen pdf-builder gewijzigd).

- [ ] **Step 4.4: Commit + push**

```bash
git add js/pdf-builder.js
git commit -m "$(cat <<'EOF'
feat(pdf): photoBlock size-parameter + §6 thumbnails 110px

photoBlock(photos, title, size=140) — backward-compatible
default. Alleen sectie §6 (Inwendige controle, 5 sub-blokken)
gebruikt size: 110 zodat de hele sectie op 1 pagina past
zonder content over te slaan. Andere secties (1, 2, 5, 7, 8)
behouden 140px voor maximale leesbaarheid.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 5: Voorpagina geen footer

**Files:**
- Modify: `js/pdf-builder.js`

- [ ] **Step 5.1: Lees huidige footer-callback**

Vind in `buildDocDefinition` (rond regel 228-233):

```js
footer: (currentPage, pageCount) => ({
  columns: [
    { text: 'Symitech B.V. · Landsweg 4, 3237 KG Vierpolders', alignment: 'left', fontSize: 8, color: '#666', margin: [40, 0, 0, 0] },
    { text: `Pagina ${currentPage} van ${pageCount}`, alignment: 'right', fontSize: 8, color: '#666', margin: [0, 0, 40, 0] }
  ]
}),
```

- [ ] **Step 5.2: Voeg conditional skip + paginanummer-correctie toe**

Vervang door:

```js
footer: (currentPage, pageCount) => {
  if (currentPage === 1) return null; // voorpagina geen footer
  return {
    columns: [
      { text: 'Symitech B.V. · Landsweg 4, 3237 KG Vierpolders', alignment: 'left', fontSize: 8, color: '#666', margin: [40, 0, 0, 0] },
      { text: `Pagina ${currentPage - 1} van ${pageCount - 1}`, alignment: 'right', fontSize: 8, color: '#666', margin: [0, 0, 40, 0] }
    ]
  };
},
```

- [ ] **Step 5.3: Run tests**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm test 2>&1 | tail -5
```

Expected: 277 passed | 0 skipped.

- [ ] **Step 5.4: Commit + push**

```bash
git add js/pdf-builder.js
git commit -m "$(cat <<'EOF'
feat(pdf): voorpagina geen footer + sectie-pagina's 1 t/m N-1

Voorpagina is dedicated rapport-cover; geen footer met
'Pagina 1 van X' meer. Sectie-pagina's (vanaf PDF-pagina 2)
tellen 1 t/m N-1 zodat gebruiker correcte inhouds-pagina-
nummers ziet ('Pagina 1 van 11' i.p.v. 'Pagina 2 van 12').

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

---

## Task 6: Visuele PDF-verificatie via Playwright

**Files:** geen lokale wijzigingen. Alleen verificatie.

- [ ] **Step 6.1: Wacht op Pages-deploy van Task 5**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3"
until [ "$(gh api repos/mauricevananraat/nen-en-858-2-v3/pages/builds/latest -q .status)" = "built" ]; do sleep 8; done
echo "DEPLOYED"
gh api repos/mauricevananraat/nen-en-858-2-v3/pages/builds/latest -q '{commit, status}'
```

Expected: status `built` op de meest recente commit (Task 5).

- [ ] **Step 6.2: Genereer test-PDF met 5-jaarlijks scenario via Playwright**

Open browser, vul testdata, switch interval, voeg foto's toe in alle 10 slots, trigger PDF en save naar Downloads. Het exacte JS-block om in browser_evaluate te gebruiken:

```js
async () => {
  // Cache-bust voor dynamic import
  const mod = await import('./js/pdf-builder.js?v=' + Date.now());
  document.getElementById('btn-testdata').click();
  const radio = document.querySelector('input[name="interval"][value="5jaarlijks"]');
  radio.checked = true;
  radio.dispatchEvent(new Event('change', { bubbles: true }));

  // 1x1 JPEG als testfoto
  const dataUrl = 'data:image/jpeg;base64,';
  const slots = ['installatie','metingen','controleput',
    'inwendig_wanden','inwendig_schotten','inwendig_coalescentie',
    'inwendig_afsluiter','inwendig_naden','lekdichtheid','coating'];
  for (const slot of slots) {
    if (!window.__inspectie.fotos[slot]) window.__inspectie.fotos[slot] = [];
    window.__inspectie.fotos[slot].push({ dataurl: dataUrl, bijschrift: `Foto ${slot}` });
  }

  const dd = mod.buildDocDefinition(window.__inspectie);
  return await new Promise(resolve => {
    window.pdfMake.createPdf(dd).getBuffer(buf => {
      const bytes = new Uint8Array(buf);
      // Download via blob
      const blob = new Blob([bytes], { type: 'application/pdf' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = 'inspectie-v33-verify.pdf';
      document.body.appendChild(a); a.click(); document.body.removeChild(a);
      setTimeout(() => URL.revokeObjectURL(url), 1000);
      // Inspect bytes
      const text = new TextDecoder('latin1').decode(bytes);
      const countMatch = text.match(/\/Type\s*\/Pages[\s\S]{0,200}?\/Count\s+(\d+)/);
      resolve({
        declaredCount: countMatch ? parseInt(countMatch[1]) : null,
        bytesSize: bytes.length
      });
    });
  });
}
```

Verwacht aantal pagina's: tussen 11 en 13 (voorpagina + 9 secties + checklist + eventuele auto-break op §6).

- [ ] **Step 6.3: Inspecteer PDF-tekst per pagina via pypdf**

Playwright slaat de PDF op in `.playwright-mcp/inspectie-v33-verify.pdf`. Inspecteer:

```bash
PDF="C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2/.playwright-mcp/inspectie-v33-verify.pdf"
py -c "
import pypdf
reader = pypdf.PdfReader(r'$PDF')
print(f'Total pages: {len(reader.pages)}')
print()
for i, page in enumerate(reader.pages, 1):
    text = page.extract_text() or ''
    lines = [l.strip() for l in text.split(chr(10)) if l.strip()]
    print(f'=== PAGE {i} ===')
    for l in lines[:3]:
        print(f'  {l[:110]}')
"
```

Verificatie-criteria:
- **Pagina 1 (voorpagina):** geen footer-tekst zichtbaar. `lines[:3]` toont alleen content van voorpagina ("INSPECTIEFORMULIER OBAS"), geen "Pagina X van Y".
- **Pagina 2+:** footer met "Symitech B.V." links en "Pagina 1 van N-1" rechts.
- **§6 op 1 pagina:** zoek in output naar `=== PAGE` waar lines bevatten "6. INWENDIGE CONTROLE". De **volgende** pagina-marker moet sectie 7 zijn ("7. LEKDICHTHEIDSTEST"). Als ertussen een pagina staat met alleen foto-tekst ("Foto's afsluiter..."), is §6 nog over 2 pagina's verdeeld → fallback acceptabel maar markeer in rapport.

- [ ] **Step 6.4: Visueel openen voor handmatige check (optioneel)**

```bash
cp "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2/.playwright-mcp/inspectie-v33-verify.pdf" "C:/Users/Maurice van Anraat/Downloads/inspectie-v33-verify.pdf"
cmd /c start "" "C:\Users\Maurice van Anraat\Downloads\inspectie-v33-verify.pdf"
```

Open de PDF voor visuele inspectie. Maurice verifieert de layout op het einde.

- [ ] **Step 6.5: Browser sluiten**

Playwright-browser sluiten zodat resources vrij komen.

---

## Task 7: STATUS.md update + finale commit

**Files:**
- Modify: `STATUS.md`

- [ ] **Step 7.1: Voeg v3.3-sectie toe onderaan STATUS.md**

Open `STATUS.md` en voeg na de bestaande v3.2-sectie toe:

```markdown
## Fase v3.3 — UX-finish + CI ✓ Afgerond op 2026-05-18

Kleine verbeteringen op basis van grondige test van v3:
- Voorpagina geen footer; sectie-pagina's tellen 1 t/m N-1 i.p.v. inclusief voorpagina
- `photoBlock` krijgt optionele `size`-parameter (default 140); §6 Inwendige controle gebruikt 110px voor 5 sub-foto-blokken zodat sectie compacter wordt
- Spinner safety-net 10s → 20s via `PDF_SPINNER_TIMEOUT_MS` constante in main.js
- GitHub Actions workflow `.github/workflows/test.yml` draait `npm ci + npm test` bij elke push naar main en alle pull requests
- Obsolete `it.skip` test in `tests/sync-ui.test.js` (uit v2 fase 5, mode-keuze via confirm) verwijderd; describe hernoemd naar 'bindSyncButtons — idempotency'

Tests: 277 groen + **0 skipped** (was 277 + 1 skipped).
```

NOTE: vervang het bestaande "## Fase 7 backlog"-blok niet — die kan blijven staan zoals het is, OF je kunt het na de v3.3-sectie houden.

- [ ] **Step 7.2: Run tests**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3" && npm test 2>&1 | tail -5
```

Expected: 277 passed | 0 skipped.

- [ ] **Step 7.3: Commit + push**

```bash
git add STATUS.md
git commit -m "$(cat <<'EOF'
docs(v3.3): markeer fase als afgerond

5 items + 1 cleanup gerealiseerd: voorpagina footer-skip,
§6 photoBlock 110px, spinner safety-net 20s, GitHub Actions
CI, skipped sync-ui-test verwijderd. Tests 277 groen + 0 skipped.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
git push
```

- [ ] **Step 7.4: Definition of Done check**

```bash
cd "C:/Users/Maurice van Anraat/Documents/.claudeV2/projects/symitech/NEN_EN858_2-v3"
npm test 2>&1 | tail -5
git log --oneline -8
git status
```

Expected:
- 277 passed | 0 skipped
- 6 v3.3-commits zichtbaar (cleanup, CI, spinner, photoBlock, footer, STATUS)
- Working tree clean

- [ ] **Step 7.5: Verifieer GitHub Actions groen**

Open `https://github.com/mauricevananraat/nen-en-858-2-v3/actions` — verwacht: alle runs (vooral de eerste na Task 2) hebben een groene checkmark. Bij rode X: open run, los issue op, commit fix.

v3.3 is afgerond ✓.

---

## Risico's en mitigaties

| Risico | Mitigatie |
|--------|-----------|
| `npm ci` op GitHub faalt door package-lock-incompatibility | Step 2.2 verifieert lokaal vóór commit |
| §6 110×110 thumbnails niet leesbaar | Task 6 (Step 6.4) opent PDF voor visuele check; fallback verhoog naar 120 of 130 |
| Footer-callback paginanummering edge-case (alleen voorpagina, geen secties) | Onmogelijk in dit project — minstens sectie 1 staat altijd in `content` |
| Skipped-test verwijderen breekt buurttest "is idempotent" | Step 1.3 valideert via `npm test`; rollback indien faal |
| Magic number-constante `PDF_SPINNER_TIMEOUT_MS` conflicteert met andere const | Naam is uniek genoeg; controleer via grep voor commit |
| Pages-cache toont oude PDF na fix | SW-update-toast informeert gebruiker; CACHE_VERSION-bump in sw.js indien snel zichtbaar moet |

---

## Niet in deze fase

- Inline delete-bevestiging
- Emoji-prefix sync-knoppen
- Custom domain
- Verdere magic-number extractie (toast: 4000, sync-ui: 1500)
- §6-layout herontwerp als 110px nog niet compact genoeg blijkt
- Build/lint-jobs in CI naast test
